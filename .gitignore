# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Per-user runtime state (strip position, etc) — not for source control
config.json
# Last-good usage snapshot persisted for fast startup display — runtime cache
usage_cache.json
usage_cache.json.tmp

# IDE
.vscode/
.idea/
*.swp

# OS junk
Thumbs.db
.DS_Store
